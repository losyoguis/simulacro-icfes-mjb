# Cambio aplicado: Dashboard institucional dentro del sistema + contraste modo noche

## Ajustes realizados

1. Se agregó nuevamente el botón **Dashboard institucional**, pero ahora queda oculto en la pantalla de inicio de sesión.
2. El botón solo aparece después de que el estudiante inicia sesión correctamente.
3. Se reforzó el contraste visual del modo noche en la app principal.
4. Se reforzó el contraste visual del modo noche en el módulo **Entrenamiento con AI Studio**.
5. Se ajustaron colores de texto, fondos, formularios, tablas, tarjetas, botones, opciones, recursos HTML, fórmulas y SVG para evitar fuentes invisibles o de bajo contraste.

## Archivos modificados

- `index.html`
- `app.js`
- `styles.css`

## Validación técnica

Se validaron sin errores de sintaxis los archivos principales:

- `app.js`
- `ai-studio-practica.js`
- `dashboard.js`
- `notebook.js`
- `data/question-bank.js`
